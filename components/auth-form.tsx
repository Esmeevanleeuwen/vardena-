import Link from "next/link";
import { login, signUp } from "@/app/actions";
export function AuthForm({mode,error,message}:{mode:"login"|"signup";error?:string;message?:string}){
 const signup=mode==="signup";
 return <div className="auth-card"><p className="eyebrow">{signup?"Word deelnemer":"Welkom terug"}</p><h1>{signup?"Maak je account.":"Log in bij Vardena."}</h1><p>{signup?"Publiceer controleerbare informatie en bouw mee aan het publieke archief.":"Ga verder met onderzoeken, bronnen en bijdragen."}</p>{error&&<p className="notice error">{error}</p>}{message&&<p className="notice success">{message}</p>}<form action={signup?signUp:login}>{signup&&<><label>Naam<input name="displayName" autoComplete="name" required placeholder="Je publieke naam"/></label><label>Gebruikersnaam<input name="username" autoComplete="username" required minLength={3} pattern="[a-zA-Z0-9_]+" placeholder="gebruikersnaam"/></label></>}<label>E-mailadres<input name="email" type="email" autoComplete="email" required placeholder="naam@voorbeeld.nl"/></label><label>Wachtwoord<input name="password" type="password" autoComplete={signup?"new-password":"current-password"} required minLength={8} placeholder="Minimaal 8 tekens"/></label><button className="button full" type="submit">{signup?"Account maken":"Inloggen"}</button></form><p className="auth-switch">{signup?"Heb je al een account?":"Nog geen account?"} <Link href={signup?"/login":"/signup"}>{signup?"Inloggen":"Account maken"}</Link></p></div>;
}
