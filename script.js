const filters=[...document.querySelectorAll(".filter")];const cards=[...document.querySelectorAll(".profile-card")];const search=document.querySelector("#searchInput");const empty=document.querySelector("#emptyState");let category="all";function update(){const query=search.value.toLowerCase().trim();let visible=0;cards.forEach(card=>{const matchesCategory=category==="all"||card.dataset.category===category;const matchesSearch=card.dataset.search.includes(query)||card.innerText.toLowerCase().includes(query);const show=matchesCategory&&matchesSearch;card.hidden=!show;if(show)visible++});empty.hidden=visible!==0}filters.forEach(button=>button.addEventListener("click",()=>{filters.forEach(item=>item.classList.remove("active"));button.classList.add("active");category=button.dataset.filter;update()}));search.addEventListener("input",update);